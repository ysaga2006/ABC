#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll ans = -1;
    ll h, w, d; cin >> h >> w >> d;
    vector<string> v(h);
    for (ll i = 0; i < h; i++) {
        cin >> v[i];
    }
    return 0;
}
