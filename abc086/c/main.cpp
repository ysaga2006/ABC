#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    pair now = make_pair(0, 0);
    int n; cin >> n;
    for (;n--;) {
        int t, x, y; cin >> t >> x >> y;
    }
    return 0;
}
